# NOTE: $(Build.SourceBranchName) takes is value from the value of the tag.
# The tag MUST ALWAYS be three digits separated by dots, e.g. "1.2.3"

variables:
  Solution.Version: $(Build.SourceBranchName)
  Package.Version: $(Build.SourceBranchName).0
